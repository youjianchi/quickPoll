SUPABASE_URL=https://your-project-id.supabase.co
# Prefer a service role key for backend usage; fall back to anon only for local development.
SUPABASE_SERVICE_ROLE_KEY=service-role-key-from-supabase
SUPABASE_ANON_KEY=anon-key-from-supabase

# Comma-separated list of allowed origins for CORS (frontend URLs)
CLIENT_ORIGIN=http://localhost:5173

# Optional: change the port the Express app listens on.
PORT=4000
